const selectedTheme = localStorage.getItem('theme') || 'light'; // Default to 'light' if 'theme' is not set

// You can also use other logic to determine the theme if needed

export default selectedTheme;